#include <iostream>
#include <limits>
#include <vector>

using namespace std;

int main() {

	vector<char> myVec;

	string hello;
	cin >> string;
	cin >> string;

	for (int i = 0; i < hello.size(); i++) {
	myVec.push_back(hello.at(i));

	for (int i = 0; i < hello.size(); i++) {
		cout << myVec[i];
	}
}

